# --- Secrets / local config (NEVER commit these) ---
.env
.env.*
!.env.example
config.js          # holds the Supabase URL + anon key for this site

# --- OS / editor junk ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Dependencies / build output (if you ever add a build step) ---
node_modules/
dist/
build/
