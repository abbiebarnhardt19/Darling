// Copy this file to `config.js` and fill in your real Supabase values.
// `config.js` is gitignored so your keys never get committed.
//
// Find these in your Supabase dashboard:
//   Project Settings -> API -> Project URL  and  Project API keys -> anon / public
//
// NOTE: For a static site (GitHub Pages) these values ship to the browser and
// are therefore PUBLIC. The "anon" key is designed to be public and MUST be
// protected by Row Level Security (RLS). NEVER put the Supabase "service_role"
// key here — it bypasses RLS and would give anyone full access to your data.

const SUPABASE_URL = "https://YOUR-PROJECT-ref.supabase.co";
const SUPABASE_ANON_KEY = "YOUR-PUBLIC-ANON-KEY";
